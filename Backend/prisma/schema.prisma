generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = ""
}

model Patient {
  id         Int      @id @default(autoincrement())
  name       String
  dob        DateTime
  phone      String
  address    String
  diagnosis  String
  createdAt  DateTime @default(now())
}